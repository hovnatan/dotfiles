-- Restored from git history (3456c4f4^) without its lazy.nvim setup
-- (lua/modules/, ~48 plugins). The few plugins now in use are listed in
-- core/plugins.lua and pinned by nvim-pack-lock.json.
require("core.utils")
require("core.options")
require("core.keymaps")
require("core.autocmd")
require("core.picker")
require("core.format")
require("core.plugins")
