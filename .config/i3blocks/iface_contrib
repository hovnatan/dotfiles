#!/bin/bash

echo eth # full text
echo eth # short text
STATUS=$(nmcli c show --active | grep ethernet | awk '{ print $1 }')
if [ -z "$STATUS" ]; then
  echo \#FF0000 # color
  exit
fi

echo \#00FF00 # color
