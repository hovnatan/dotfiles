# shellcheck settings for this repo.  CI (.github/workflows/shellcheck.yml)
# runs exactly this, at warning level; info/style notes are advisory:
#   git ls-files '*.sh' | grep -v '^scripts/countdown.sh$' | xargs shellcheck -S warning
# scripts/countdown.sh is zsh, which shellcheck does not support; skip it.
external-sources=true
